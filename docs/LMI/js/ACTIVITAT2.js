// Creem les variables
let numero = 42;              // Un número
let text = "Hola, món!";      // Una cadena de text
let esCert = true;            // Un valor lògic (boolean)

console.log(typeof numero);   // Mostra el tipus de la variable 'numero'
console.log(typeof text);     // Mostra el tipus de la variable 'text'
console.log(typeof esCert);   // Mostra el tipus de la variable 'esCert'